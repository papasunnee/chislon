import React, { Fragment } from "react";
import Countries from "../../components/Countries";

const Country = () => {
  return (
    <Fragment>
      <Countries />
    </Fragment>
  );
};
export default Country;
